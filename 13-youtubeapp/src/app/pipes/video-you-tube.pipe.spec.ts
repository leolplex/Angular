import { VideoYouTubePipe } from './video-you-tube.pipe';

describe('VideoYouTubePipe', () => {
  it('create an instance', () => {
    const pipe = new VideoYouTubePipe();
    expect(pipe).toBeTruthy();
  });
});
